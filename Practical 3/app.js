const { add, subtract } = require('Practical 3/Math.js');

const sum = add(10, 5);
const difference = subtract(10, 5);

console.log(`The sum of 10 and 5 is: ${sum}`);
console.log(`The difference of 10 and 5 is: ${difference}`);
